version: '3.8'

services:
  ckd-api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./AdaBoost.joblib:/app/AdaBoost.joblib
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped